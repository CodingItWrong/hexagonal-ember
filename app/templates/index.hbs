<h1>Todos</h1>

<form {{on "submit" createTodo}}>
  <Input
    type="text"
    @value={{this.newTodoName}}
  />
  <button type="submit">Create</button>
</form>

<ul>
  {{#each @model as |todo|}}
    <li>{{todo.name}}</li>
  {{/each}}
</ul>
